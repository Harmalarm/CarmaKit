VERSION 4
// Sub member: Master-crush data

1                           // Number of 'X mins' entries.
.08
1                           // Number of 'X maxs' entries.
-.08
1                           // Number of 'Y mins' entries.
.43
1                           // Number of 'Y maxs' entries.
.17
1                           // Number of 'Z mins' entries.
.26
1                           // Number of 'Z maxs' entries.
-.25

1.000000                    // Bendability factor
-0.200000                   // Bend point Z min
0.200000                    // Bend point Z max
1.000000                    // Snappability factor
0.000000                    // Y split position
-0.087,0.532,-0.154        // Driver position


12					// Number of 'Crush data' entries.

f_door_l1
	normal
	flap
		20
		69
		44
		1
		normal
		box
	0

f_door_l3
	normal
	flap
		0
		66
		47
		0
		easy
		box
	0

f_door_r1
	normal
	flap
		22
		70
		9
		1
		normal
		box
	0

f_door_r3
	normal
	flap
		1
		66
		8
		0
		easy
		box
	0

f_hood
	soft
	flap
		3
		6
		16
		0
		very_easy
		box
	0

d_door_l2
	normal
	detach
		normal
		normal
		box
	0

d_door_r2
	normal
	detach
		normal
		normal
		box
	0

d_bumper_f
	normal
	detach
		normal
		stubborn
		box
	0

d_bumper_r
	normal
	detach
		normal
		stubborn
		box
	0

chassis
	very_hard
	boring
	0

driver
	uncrushable
	boring
	0

ihorse.act
	normal
	boring
	5

		ihorsel1.mat
			ihorsel1		// pixelmap to use when intact
			1				// Number of levels
			0.002				// trigger threshold (default if zero)
			0				// flags

				// Connotations:
				
				1				// number of possible sounds
				4520			// sound ID

				1				// Shrapnel count
				shards			// Shrapnel type
				0,0				// Min,max speed towards car
				1.0				// Impactee velocity factor
				10.0			// Random velocity (max)
				2.0				// Random up velocity (max)
				0				// Random normal velocity (max)
				100.0			// Random spin rate (max)
				0.75,1.25		// Min time, Max time
				0.005			// Min cut length
				0x01			// flags
				RLFRAG.MAT		// name of shrapnel material
				
				0										// Explosion size
				none									// Slick material
				0				// Number of non-cars activated
				0				// Radius of side-effect smashes
				0				// Extensions flags
				0				// Room turn on code
				none			// Award code
				0				// No run-time variable changes

			1				// Number of pixelmaps
			ihorsel2	// pixelmap to use when smashed

		ihorsel2.mat
			ihorsel1		// pixelmap to use when intact
			1				// Number of levels
			0.002				// trigger threshold (default if zero)
			0				// flags

				// Connotations:
				
				1				// number of possible sounds
				4520			// sound ID

				1				// Shrapnel count
				shards			// Shrapnel type
				0,0				// Min,max speed towards car
				1.0				// Impactee velocity factor
				10.0			// Random velocity (max)
				2.0				// Random up velocity (max)
				0				// Random normal velocity (max)
				100.0			// Random spin rate (max)
				0.75,1.25		// Min time, Max time
				0.005			// Min cut length
				0x01			// flags
				RLFRAG.MAT		// name of shrapnel material
				
				0										// Explosion size
				none									// Slick material
				0				// Number of non-cars activated
				0				// Radius of side-effect smashes
				0				// Extensions flags
				0				// Room turn on code
				none			// Award code
				0				// No run-time variable changes

			1				// Number of pixelmaps
			ihorsel2	// pixelmap to use when smashed

		ihorsel3.mat
			ihorsel1		// pixelmap to use when intact
			1				// Number of levels
			0.002				// trigger threshold (default if zero)
			0				// flags

				// Connotations:
				
				1				// number of possible sounds
				4520			// sound ID

				1				// Shrapnel count
				shards			// Shrapnel type
				0,0				// Min,max speed towards car
				1.0				// Impactee velocity factor
				10.0			// Random velocity (max)
				2.0				// Random up velocity (max)
				0				// Random normal velocity (max)
				100.0			// Random spin rate (max)
				0.75,1.25		// Min time, Max time
				0.005			// Min cut length
				0x01			// flags
				RLFRAG.MAT		// name of shrapnel material
				
				0										// Explosion size
				none									// Slick material
				0				// Number of non-cars activated
				0				// Radius of side-effect smashes
				0				// Extensions flags
				0				// Room turn on code
				none			// Award code
				0				// No run-time variable changes

			1				// Number of pixelmaps
			ihorsel2	// pixelmap to use when smashed

		ihorsel4.mat
			ihorsel1		// pixelmap to use when intact
			1				// Number of levels
			0.002				// trigger threshold (default if zer o)
			0				// flags

				// Connotations:
				
				1				// number of possible sounds
				4520			// sound ID

				1				// Shrapnel count
				shards			// Shrapnel type
				0,0				// Min,max speed towards car
				1.0				// Impactee velocity factor
				10.0			// Random velocity (max)
				2.0				// Random up velocity (max)
				0				// Random normal velocity (max)
				100.0			// Random spin rate (max)
				0.75,1.25		// Min time, Max time
				0.005			// Min cut length
				0x01			// flags
				RLFRAG.MAT		// name of shrapnel material
				
				0										// Explosion size
				none									// Slick material
				0				// Number of non-cars activated
				0				// Radius of side-effect smashes
				0				// Extensions flags
				0				// Room turn on code
				none			// Award code
				0				// No run-time variable changes

			1				// Number of pixelmaps
			ihorsel2	// pixelmap to use when smashed

		ihorsews.mat
			ihorsews	// pixelmap to use when intact
			1				// Number of levels
			0.002			// trigger threshold (default if zero)
			0				// flags

				// Connotations:
				
				1				// number of possible sounds
				4500			// sound ID

				1				// Shrapnel count
				shards			// Shrapnel type
				0,0				// Min,max speed towards car
				1.0				// Impactee velocity factor
				10.0			// Random velocity (max)
				2.0				// Random up velocity (max)
				0				// Random normal velocity (max)
				100.0			// Random spin rate (max)
				0.75,1.25		// Min time, Max time
				0.005			// Min cut length
				0				// flags
				SCRN4		// name of shrapnel material
				
				0				// Explosion size
				none			// Slick material
				0				// Number of non-cars activated
				0				// Radius of side-effect smashes
				0				// Extensions flags
				0				// Room turn on code
				none			// Award code
				0				// No run-time variable changes

			1				// Number of pixelmaps
			ihorsewx		// pixelmap to use when smashed level 5